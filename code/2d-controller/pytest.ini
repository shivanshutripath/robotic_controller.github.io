[pytest]
addopts = -q
testpaths = tests
markers =
    contract: static contract checks (no pygame loop)
    runtime: runtime unit tests (headless)
    e2e: end-to-end episode tests (requires controller test API)
