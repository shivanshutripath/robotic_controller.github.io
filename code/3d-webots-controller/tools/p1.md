from string import Template

PROMPT = Template(r"""
You are generating a Python controller for a Webots E-puck robot.

OUTPUT: A single valid Python file. No markdown. No explanations. Only code.

═══════════════════════════════════════════════════════════
STEP 1: FILE STRUCTURE
═══════════════════════════════════════════════════════════

Create a Python file with these 6 functions at MODULE LEVEL (not nested):

1. parse_world(world_text, params_text="") -> dict
2. build_grid(bounds, obstacles, resolution=0.1, inflation=0.0) -> list
3. astar(grid, start_cell, goal_cell) -> list
4. compute_wheel_speeds(pose_xy, yaw, waypoint_xy, prox) -> tuple
5. run_episode(...) -> dict
6. main() -> None

End with: if __name__ == "__main__": main()

═══════════════════════════════════════════════════════════
STEP 2: IMPORT RULES
═══════════════════════════════════════════════════════════

AT TOP OF FILE (module level):
- import re
- import heapq  
- import math

INSIDE run_episode() and main() ONLY:
- from controller import Supervisor

NEVER put Webots imports at top of file.

═══════════════════════════════════════════════════════════
STEP 3: parse_world FUNCTION
═══════════════════════════════════════════════════════════

Input: world_text (string containing the .wbt file)
Output: dict with keys: plane, start, goal, obstacles, bounds

IMPORTANT PARSING NOTES:
- The world file has MULTIPLE nodes with translation fields
- You must find the SPECIFIC nodes, not just any translation

A) ROBOT START - Find the E-puck robot:
   The E-puck section looks like:
     E-puck {
       translation -0.423352 0.688226 0
       rotation ...
       name "e-puck"
       controller ...
     }
   
   Use regex that matches E-puck followed by its translation:
   Pattern: E-puck\s*\{[^}]*?translation\s+([-\d.e+]+)\s+([-\d.e+]+)
   
   EXPECTED: x ≈ -0.423352, y ≈ 0.688226

B) GOAL - Find DEF GOAL Solid:
   The GOAL section looks like:
     DEF GOAL Solid {
       translation 0.54125 -0.0579839 0.05
       ...
     }
   
   Use regex that matches DEF GOAL followed by translation:
   Pattern: DEF\s+GOAL\s+\w+\s*\{[^}]*?translation\s+([-\d.e+]+)\s+([-\d.e+]+)
   
   EXPECTED: x ≈ 0.54125, y ≈ -0.0579839

C) OBSTACLES - Find WoodenBox and Rock nodes:
   
   WoodenBox example:
     WoodenBox {
       translation 0.194803 0.0218687 0.05
       name "wooden box"
       size 0.2 0.2 0.2
     }
   Extract: x=0.194803, y=0.0218687, sx=0.2, sy=0.2
   
   Rock example:
     Rock {
       translation X Y Z
       scale S
     }
   Extract: x=X, y=Y, sx=S*0.15, sy=S*0.15

D) BOUNDS - Calculate bounding box:
   Collect all x,y from start, goal, and obstacles
   Add 0.5 margin
   Return: [x_min-0.5, x_max+0.5, y_min-0.5, y_max+0.5]

E) Return:
   {"plane": "xy", "start": {"x":..,"y":..}, "goal": {"x":..,"y":..}, 
    "obstacles": [...], "bounds": [x_min,x_max,y_min,y_max]}

═══════════════════════════════════════════════════════════
STEP 4: build_grid FUNCTION
═══════════════════════════════════════════════════════════

Input: bounds [x_min, x_max, y_min, y_max], obstacles list, resolution, inflation
Output: 2D list of integers (0=free, 1=occupied)

Steps:
1. Calculate grid dimensions from bounds and resolution
2. Create 2D list filled with zeros
3. For each obstacle, mark overlapping cells as 1
4. Return the grid

═══════════════════════════════════════════════════════════
STEP 5: astar FUNCTION
═══════════════════════════════════════════════════════════

Input: grid (2D list), start_cell (i,j), goal_cell (i,j)
Output: list of (i,j) tuples from start to goal

Standard A* with 8-connectivity.

═══════════════════════════════════════════════════════════
STEP 6: compute_wheel_speeds FUNCTION - CAREFULLY READ
═══════════════════════════════════════════════════════════

Input: pose_xy (x,y), yaw (radians), waypoint_xy (x,y), prox (list of 8 values)
Output: (left_speed, right_speed) tuple

CAREFULLY IMPLEMENT THE STEERING LOGIC:

1. Check front sensors (indices 0, 1, 6, 7). If any > 100, do recovery:
   Return (negative, positive) or (positive, negative) to turn away

2. Calculate target angle:
   dx = waypoint_xy[0] - pose_xy[0]
   dy = waypoint_xy[1] - pose_xy[1]
   target_angle = math.atan2(dy, dx)

3. Calculate heading error:
   error = target_angle - yaw
   Normalize to [-pi, pi]

4. STEERING LOGIC (this is critical):
   base_speed = 6.0
   turn_gain = 3.0
   
   left_speed  = base_speed - turn_gain * error
   right_speed = base_speed + turn_gain * error

   WHY THIS WORKS:
   - error > 0 means waypoint is to the LEFT
     → right_speed increases, left_speed decreases
     → robot turns LEFT ✓
   
   - error < 0 means waypoint is to the RIGHT  
     → left_speed increases, right_speed decreases
     → robot turns RIGHT ✓

   - error ≈ 0 means waypoint is ahead
     → both wheels equal
     → robot goes straight ✓

5. Clamp speeds to [-6.28, 6.28]

6. Return (left_speed, right_speed)

═══════════════════════════════════════════════════════════
STEP 7: run_episode and main FUNCTIONS
═══════════════════════════════════════════════════════════

run_episode():
- Import Supervisor inside the function
- Return {"success": bool, "steps": int}

main():
- Import Supervisor inside the function  
- Entry point for Webots

═══════════════════════════════════════════════════════════
WORLD FILE CONTENT
═══════════════════════════════════════════════════════════

Path: $WORLD_WBT

$WORLD_WBT_TEXT

═══════════════════════════════════════════════════════════
VERIFICATION - YOUR CODE MUST PRODUCE THESE VALUES
═══════════════════════════════════════════════════════════

parse_world should return approximately:
- start["x"] ≈ -0.423352 (tolerance: ±0.03)
- start["y"] ≈ 0.688226 (tolerance: ±0.03)
- goal["x"] ≈ 0.54125 (tolerance: ±0.03)
- goal["y"] ≈ -0.0579839 (tolerance: ±0.03)
- len(obstacles) >= 5

compute_wheel_speeds should:
- When waypoint is LEFT (positive Y): right > left
- When waypoint is RIGHT (negative Y): left > right
- When front blocked: recovery motion

═══════════════════════════════════════════════════════════
REPAIR FEEDBACK
═══════════════════════════════════════════════════════════
$AUTO_REPAIR

═══════════════════════════════════════════════════════════
OUTPUT NOW
═══════════════════════════════════════════════════════════

Generate the complete Python file. No markdown. No explanations. Only valid Python code.
""")